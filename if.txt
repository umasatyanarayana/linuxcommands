command:ifcommand
use:if is a command in Linux which is used to execute commands based on conditions. 
     The 'if COMMANDS' list is executed. If its status is zero, then the 'then COMMANDS' list is executed.